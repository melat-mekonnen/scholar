"use client"

/**
 * Same nav items and behavior as main `StudentPortalSidebar` (billing, i18n, active routes).
 * Use on ui-update2 pages that keep the inline emerald shell instead of `StudentPortalFrame`.
 */
export { StudentPortalSidebar as StudentPortalInlineAside } from "@/components/student-portal/student-portal-sidebar"
